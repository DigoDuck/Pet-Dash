# Frontend (React + Vite + Tailwind)

App React ainda a scaffoldar. Consome a API DRF do `backend/`.

Protótipos do Lovable são só referência visual (dados mockados, sem Supabase). Antes de portar qualquer componente, rodar `grep -r "supabase"` no export e limpar. Ver `CLAUDE.md`.
